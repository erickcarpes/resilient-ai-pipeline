// Meeting entity — re-exports from @pipeline/shared for use within the gateway
// Workers import Meeting directly from '@pipeline/shared'
export { type Meeting, MeetingStatus } from '@pipeline/shared';
